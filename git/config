[user]
	email = kalvinpearce@gmail.com
	name = Kalvin Pearce
[init]
	defaultBranch = main

[core]
    pager = delta
[interactive]
    diffFilter = delta --color-only
[delta]
    navigate = true  # use n and N to move between diff sections
[merge]
    conflictstyle = diff3
[diff]
    colorMoved = default
[safe]
	directory = *
[commit]
	template = /home/kalvin/.config/git/commit-template.txt
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
